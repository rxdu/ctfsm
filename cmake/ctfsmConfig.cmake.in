@PACKAGE_INIT@

# ctfsm is header-only with no external dependencies, so there is nothing to
# find_dependency() here. Just expose the imported targets.
include("${CMAKE_CURRENT_LIST_DIR}/ctfsmTargets.cmake")

check_required_components(ctfsm)
